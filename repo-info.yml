# must be defined in [] yaml structure for bash script to work
templates: [golang]

project:
  name: cloudflare-exporter

deploy:
  stage:
    enabled: true
    namespace: infra
  prod:
    enabled: true
    namespace: infra